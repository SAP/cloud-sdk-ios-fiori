import Foundation

public enum RowAlignment: Sendable {
    /// texts are aligned with baseline
    case baseline
    
    /// default alignment
    case top
    
    /*
      add these if required in future
     case center
     case bottom
      */
}
