//
//  CardCollection.swift
//  DevTest
//
//  Created by Stadelman, Stan on 1/23/20.
//  Copyright © 2020 sstadelman. All rights reserved.
//

import SwiftUI
import ObservableArray
/*
struct CardCollection: View {
    
    @EnvironmentObject var model: ObservableArray<Manifest>
    let type: String
    
    var body: some View {
        Text(/*@START_MENU_TOKEN@*/"Hello, World!"/*@END_MENU_TOKEN@*/)
    }
    
    func cards() -> [Manifest] {
        return model.filter { $0.card.description == type }
    }
    
    func makeBody() -> AnyView {
        let manifests = cards()
        HStack {
            ForEach(manifests) { manifest in
                
            }
        }
    }
}

struct CardCollection_Previews: PreviewProvider {
    static var previews: some View {
        CardCollection(type: "Object")
    }
}
*/
